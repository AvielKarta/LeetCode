#include <stdio.h>
#include "implementaions.h"
#include "tools.h"


void arrays(void)
{
	/*1920. build-array-from-permutation

	int nums[] = { 0, 2, 1, 5, 3, 4 };
	int expected[] = { 0, 1, 2, 4, 5, 3 };
	int size = sizeof(nums) / sizeof(int);
	int* returnSize = &size;
	int* res = nums;
	res = buildArray(nums, size, returnSize);
	test_arrays_match(res, expected, size);
	
	*/

	/*1684. Count the Number of Consistent Strings
	char* allowed = "ab";
	char* words[] = {"ad", "bd", "aaab", "baa", "badab"};
	test_int_results(countConsistentStrings(allowed, words, 5), 2);
	*/
}

void strings(void)
{

}




int  main(void)
{
	arrays();
	strings();
}